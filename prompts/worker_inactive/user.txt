Round t = {{step}}. You have been assigned role IN (Inactive).

No actions are required for agent {{agent_id}} this round. Return an empty
action list.

Context (signals, memory — for awareness only):
{{context_json}}
