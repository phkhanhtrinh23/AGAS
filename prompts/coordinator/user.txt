Round t = {{step}} of T = {{total_steps}}.

Pick exactly one strategy from {S1_VICTIM_PROBE, S2_BRIDGE_BUILDING, S3_WARM_UP,
S4_FIRST_PUSH, S5_SILENT_SLOWDOWN, S6_PROFILE_CLEANUP, S7_SAFE_REPLACEMENT,
S8_MAIN_ATTACK}, then assign exactly one role from {PR, SN, CA, IN} to every
worker listed below.

Black-box observation for this round:
{{context_json}}

Decide using the trigger rules in the system prompt. Return JSON only.
